# Mizan AI — سجل الإصلاحات (v2.1)

مراجعة شاملة للنسخة `Mizan_AI_Final_v2` مع إصلاح المشاكل المؤكدة وإعادة التحقق الكامل.

---

## ✅ المشاكل التي تم إصلاحها

### 1. إعادة بناء الفهرس تكسر تصفية المصادر (`scripts/embed_and_index.py`)
- **المشكلة:** كان سكربت البناء لا يكتب حقل `doc_id` في ميتاداتا ChromaDB (كان يعتمد على سكربت ترقيع يدوي `backfill_doc_id_metadata.py` غير المذكور في خطوات README). أي إعادة بناء فهرس باتباع التعليمات الموثقة كانت تجعل قائمة "تصفية المصادر" في الواجهة تُرجع نتائج فارغة بصمت.
- **الإصلاح:** إضافة `doc_id` إلى الميتاداتا مباشرةً، وتصحيح القيمة الافتراضية الخاطئة للسنة (`2010` → `0`).

### 2. اعتمادية OCR مفقودة (`requirements.txt`)
- **المشكلة:** 6 من أصل 12 وثيقة تُعالج عبر `winocr` (Windows Media OCR) في `scripts/step2_extract.py`، لكن الحزمة لم تكن في `requirements.txt` — تثبيت جديد يفشل بخطأ `ModuleNotFoundError` عند تشغيل خط المعالجة على الوثائق الممسوحة.
- **الإصلاح:** إضافة `winocr>=0.1.6; sys_platform == "win32"`.

### 3. إعدادات Tesseract ميتة ومضللة (`config.py`)
- **المشكلة:** `TESSERACT_CMD` و`OCR_LANG` و`OCR_DPI` غير مستخدمة في أي مكان؛ الكود الفعلي يستخدم winocr — تضليل لمن يعدّل الإعدادات.
- **الإصلاح:** حذف المفاتيح الميتة وتوثيق آلية OCR الفعلية في التعليق.

### 4. حقن HTML في بطاقات السند (`app.py`)
- **المشكلة:** نصوص المواد القانونية تُحقن خاماً داخل HTML مع `unsafe_allow_html=True` — أي محرف `<` أو `>` في النص يكسر التنسيق ويسمح بحقن HTML.
- **الإصلاح:** تهريب جميع الحقول النصية عبر `html.escape`.

### 5. قائمة مواد مضللة في النمط المدمج (`mizan/generator.py`)
- **المشكلة:** السطر الافتتاحي كان يكرر أرقام المواد بدون أسماء القوانين: «المادة (34) و المادة (34) و المادة (344)».
- **الإصلاح:** إزالة التكرار (مادة + قانون) وذكر اسم القانون مع كل مادة.

### 6. عنوان سند فارغ في CLI (`chat_cli.py`)
- **المشكلة:** طباعة قسم "السند والمصادر" فارغاً عند رفض خارج النطاق/لا إجابة.
- **الإصلاح:** طباعة القسم فقط عند وجود سند.

### 7. مجلدات فهرس يتيمة داخل `data/chroma_db`
- **المشكلة:** مجلدا HNSW قديمان (`149c248b-…` و`d3a4f52d-…` بحجم ~2MB) غير مرجعيين في `chroma.sqlite3` — بقايا حذف مجموعات سابقة.
- **الإصلاح:** استثناءهما من الحزمة المسلَّمة (قاعدة البيانات النشطة `219c023a-…` كاملة وسليمة).

---

## 🔍 فحوصات تمت ونتائجها سليمة (بلا تغيير)

| الفحص | النتيجة |
|---|---|
| تطابق `all_chunks.json` مع فهرس ChromaDB | 2,929 مقطعاً متطابقة تماماً، لا معرفات مكررة |
| اكتمال `doc_id` في الميتاداتا المخزنة | 2,929/2,929 (التوزيع مطابق للوثائق الـ12) |
| اختبارات بوابات الملاءمة `evaluation/test_gates.py` | 14/14 ناجحة |
| فحص صياغة جميع ملفات Python | سليم |
| معايرة `has_text_layer` في الإعدادات | صحيحة — طبقات النص في PDFs "الممسوحة" تالفة فعلاً (ترميز خطوط معطوب) وOCR هو الخيار الصحيح |

## ⚠️ قيود معروفة (موثقة في README — تتطلب مصادر أفضل لا إصلاحاً برمجياً)

1. **قرار 888/2023:** 6 مواد فقط مفهرسة (النسخة المتاحة ممسوحة بجودة منخفضة وست صفحات).
2. **جودة OCR:** بعض نصوص الوثائق الممسوحة تحوي أخطاء تعرف ضوئي (مثل "إثخاذ" بدل "اتخاذ") — موروثة من المصدر.
3. **إعادة معالجة OCR:** تتطلب ويندوز 10/11 (winocr غير متاح على Linux/macOS).
